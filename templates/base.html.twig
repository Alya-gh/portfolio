<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>{% block title %}Alya — Portfolio{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>


  <style>
    body {
      background-color: #0b0b0b; /* Fond noir pour toutes les pages */
      color: #fff; /* Texte blanc par défaut */
    }

    /* Navbar */
    .navbar-brand { margin-left: 20px; }
    .navbar-nav .nav-link {
      margin-left: 15px;
      margin-right: 15px;
      border-radius: 20px;
      padding: 10px 18px;
      font-size: 1.1rem;
      transition: all 0.3s;
      color: #fff !important;
    }
    .navbar-nav .nav-link:hover,
    .navbar-nav .nav-link:active,
    .navbar-nav .nav-link.active {
      background-color: #d59bf6;
      color: white !important;
    }

    /* Ligne séparatrice blanche */
    .separator {
      height: 3px;
      background-color: white;
      margin-top: 5px;
      margin-bottom: 20px;
    }
  </style>

  {% block styles %}{% endblock %}
</head>

<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-black py-3 shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="{{ path('home') }}" style="font-size:1.8rem;">PORTFOLIO</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Accueil</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('apropos') }}">À propos</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('parcours') }}">Parcours</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('projets_experiences') }}">Projets&Expériences</a></li>
        <li class="nav-item"><a class="nav-link" href="{{ path('contact') }}">Contact</a></li>
      </ul>
    </div>
  </div>
</nav>

<div class="separator"></div>

<main class="container py-5">
  {% block body %}{% endblock %}
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% block scripts %}{% endblock %}
</body>
</html>

<div class="footer-note">
    © 2025 ODDOZ Alya — Contenu protégé, copie interdite
</div>